# Save draft can privatize a Public Assembly

After confirmation, Save draft on a Public Assembly makes it Private and incomplete and removes it from the public catalog. Nested Products and impact EPDs stay Public (ADR 0003).

We rejected forbidding Save draft on Public (broken edits could not be persisted) and rejected forking a Private copy while leaving the Public Assembly unchanged.
