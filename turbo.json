{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^check"]
    },
    "check": {
      "dependsOn": ["^tsc", "^lint", "^format"]
    },
    "dev": {
      "persistent": true,
      "cache": false
    }
  }
}