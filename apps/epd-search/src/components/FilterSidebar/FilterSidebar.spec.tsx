import { describe, it, expect } from 'vitest'
import { FilterSidebar } from './FilterSidebar'

describe('FilterSidebar', () => {
  it('should be defined', () => {
    expect(FilterSidebar).toBeDefined()
  })
})
