import { describe, it, expect } from 'vitest'
import { LCAxStatisticsCard } from './LCAxStatisticsCard'

describe('LCAxStatisticsCard', () => {
  it('should be defined', () => {
    expect(LCAxStatisticsCard).toBeDefined()
  })
})
