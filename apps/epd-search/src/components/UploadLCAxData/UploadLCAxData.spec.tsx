import { describe, it, expect } from 'vitest'
import { UploadLCAxData } from './UploadLCAxData'

describe('UploadLCAxData', () => {
  it('should be defined', () => {
    expect(UploadLCAxData).toBeDefined()
  })
})
